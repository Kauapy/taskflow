# Copie para .env (na raiz do projeto). É o frontend (Vite).
# Nunca comite o .env com valores reais — já está no .gitignore.

# URL do backend Taskflow (server/). Em dev, o padrão abaixo serve.
# Em produção, aponte para o backend publicado (ex.: https://api.seuapp.com).
VITE_API_URL=http://localhost:4000
